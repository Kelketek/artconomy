[build-system]
requires = ["maturin>=1,<2", "cffi>=1.16.0,<2"]
build-backend = "maturin"

[project]
name = "line_items"
requires-python = ">=3.11"
version = "0.1.0"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
]
