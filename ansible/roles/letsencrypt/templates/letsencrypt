#! /bin/bash
letsencrypt --agree-tos --webroot --force --webroot-path /home/{{ env_prefix }}/artconomy/ {% for name in domains %}-d {{ name }} {% endfor %} --email {{ admin_email }} certonly &> /dev/null
/etc/init.d/nginx reload &> /dev/null
